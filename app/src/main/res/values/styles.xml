<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextAppearence.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyAVLoadingIndicatorSmallStyle" parent="AVLoadingIndicatorView.Small">
        <item name="minWidth">27dip</item>
        <item name="maxWidth">27dip</item>
        <item name="minHeight">27dip</item>
        <item name="maxHeight">27dip</item>
    </style>

    <style name="TransparentActionBar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
    </style>

    <style name="ToolBarTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="editTextLabel">
        <item name="android:textColor">@android:color/tab_indicator_text</item>
        <item name="android:textSize">@dimen/font_size_16dp</item>
        <item name="android:layout_marginLeft">@dimen/edit_text_padding</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>
</resources>
